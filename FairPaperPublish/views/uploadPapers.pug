extends layout

block content
  h1= title
  p Welcome to #{title}

  div.body#dynamicContent
    body
      form(ref=uploadForm, id=uploadForm, action='/uploadPapers/upload', method='POST',encType="multipart/form-data")
          input(type='file', name="fileUpload")
          input(type='submit', value='Upload')

